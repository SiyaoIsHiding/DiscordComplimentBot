{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "tsc",
            "args": ["--project", "${workspaceFolder}/tsconfig.json"]
        }
    ]
}